{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "common.schema.json",
  "title": "Common Shared Types",
  "description": "Shared definitions referenced by module schemas",

  "definitions": {
    "period": {
      "type": "object",
      "description": "A time range with YYYY-MM formatted dates. end is null if ongoing.",
      "properties": {
        "start": { "type": "string", "pattern": "^\\d{4}-\\d{2}$" },
        "end": { "oneOf": [{ "type": "string", "pattern": "^\\d{4}-\\d{2}$" }, { "type": "null" }] }
      },
      "required": ["start", "end"]
    },
    "tags": {
      "type": "array",
      "description": "Free-form keyword labels used by agents to filter and match content",
      "items": { "type": "string" }
    }
  }
}
